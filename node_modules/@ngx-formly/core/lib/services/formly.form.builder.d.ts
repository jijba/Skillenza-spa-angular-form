import { FormGroup, FormArray } from '@angular/forms';
import { FormlyConfig } from './formly.config';
import { FormlyFieldConfig, FormlyFormOptions } from '../components/formly.field.config';
import { FormlyFormExpression } from './formly.form.expression';
export declare class FormlyFormBuilder {
    private formlyConfig;
    private formlyFormExpression;
    private formId;
    constructor(formlyConfig: FormlyConfig, formlyFormExpression: FormlyFormExpression);
    buildForm(form: FormGroup | FormArray, fields: FormlyFieldConfig[], model: any, options: FormlyFormOptions): void;
    private _buildForm(form, fields, options);
    private registerFormControls(form, fields, options);
    private initFieldExpression(field, options);
    private initFieldOptions(field);
    private initFieldAsyncValidation(field);
    private initFieldValidation(field);
    private addFormControl(form, field, model, path);
    private addControl(form, key, formControl, field?);
    private getValidation(opt, value);
    private wrapNgValidatorFn(field, validator);
    private initFieldWrappers(field);
    private mergeTemplateManipulators(source, target);
}
